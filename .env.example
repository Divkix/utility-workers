# AI Parser Worker - Required for OpenRouter via Cloudflare AI Gateway
CF_AI_GATEWAY_ACCOUNT_ID=your_cloudflare_account_id
CF_AI_GATEWAY_ID=your_gateway_id
CF_AIG_AUTH_TOKEN=your_gateway_auth_token

# Optional: Direct OpenRouter (if not using AI Gateway)
# OPENROUTER_API_KEY=your_openrouter_key
